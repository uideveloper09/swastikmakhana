/** Client-safe API helpers — server fetch lives in api-server.ts */
export { formatPrice, formatRatingCount } from "@/lib/format";
export { categorySlugToPath, pathToUrl, slugToPath } from "@/lib/paths";
